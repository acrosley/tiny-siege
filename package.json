{
  "name": "tiny-siege",
  "version": "1.0.0",
  "private": true,
  "description": "An offline, simultaneous-turn fortress duel for two local players.",
  "scripts": {
    "test": "node --test tests/engine.test.cjs",
    "build": "node scripts/build.cjs",
    "start": "node scripts/serve.cjs",
    "test:browser": "npx playwright test",
    "format": "prettier --write index.html style.css app.js scene.js engine.js scripts/*.cjs tests/**/*.cjs playwright.config.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
